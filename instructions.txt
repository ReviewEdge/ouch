# Create Config 
*you actually have to put config in sample, so this doesn't completely work:
cp ~/ouch/docs/config_example.py config.py


# Install list

pip3 install telethon
pip3 install forex-python

# DB
sudo apt install sqlite3

mkdir db
sqlite3 pimylifeupSQLite.db

CREATE TABLE "spends" ("ID"	INTEGER UNIQUE, "Cost"	NUMERIC, "Description"	TEXT, "Category"	TEXT, "Date"	TEXT, PRIMARY KEY("ID" AUTOINCREMENT));
